import { EmptyState } from '@/pages/DashboardPage'

/** Route de démonstration, volontairement vide. */
export function DemoPage() {
  return <EmptyState title="Démo" description="Écran de démonstration, sans contenu." />
}
